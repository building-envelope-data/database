query OpenIdConnectApplications($ids: [String!]!) {
  connection: openIdConnectApplications(where: { clientId: { in: $ids } }) {
    edges {
      node {
        uuid
        id: clientId
        displayName
      }
    }
  }
}
